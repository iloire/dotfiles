# Security hardening against supply chain attacks
# TODO: min-release-age=7d — blocked by npm/cli#9005 (breaks with ~ version ranges), re-enable when fixed
registry=https://registry.npmjs.org/
strict-ssl=true
ignore-scripts=true
save-exact=true
package-lock=true
audit=true
audit-level=moderate
fund=false
