#!/bin/sh
# Category: git
# Description: Delete branch locally and on remote
#
# Nukes a branch locally and on the origin remote.
#
# $1 - Branch name.
#
# Examples
#
#   git nuke add-git-nuke

git branch -D $1
git push origin :$1
